apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: cake-delight
  labels:
    app.kubernetes.io/name: order-service
    app.kubernetes.io/part-of: cake-delight
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: order-service
  ports:
    - name: http
      port: 8083
      targetPort: http

apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: cake-delight
  labels:
    app.kubernetes.io/name: notification-service
    app.kubernetes.io/part-of: cake-delight
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: notification-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: notification-service
        app.kubernetes.io/part-of: cake-delight
    spec:
      enableServiceLinks: false
      containers:
        - name: notification-service
          image: akashgupta2233/cake-delight-notification-service:v1.0.0
          ports:
            - containerPort: 8085
              name: http
          envFrom:
            - configMapRef:
                name: database-hosts
            - configMapRef:
                name: rabbitmq-host
            - secretRef:
                name: database-credentials
            - secretRef:
                name: rabbitmq-credentials
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 40
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi

apiVersion: v1
kind: Service
metadata:
  name: rating-service
  namespace: cake-delight
  labels:
    app.kubernetes.io/name: rating-service
    app.kubernetes.io/part-of: cake-delight
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rating-service
  ports:
    - name: http
      port: 8084
      targetPort: http
